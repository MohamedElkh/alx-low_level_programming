#include "main.h"
#include <stdio.h>

/**
 * mul -  a function that multiplies two integers.
 * @a: the first character to be checked .
 * @b: the second character to be checked .
 *
 * Description: a function that multiplies two integers.
 * Return: the result of multiplies two integers.
 */

int mul(int a, int b)
{
	return (a * b);
}
